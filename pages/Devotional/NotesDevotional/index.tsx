import { StyleSheet, Text, View } from "react-native";
import React from "react";

const NotesDevotional = () => {
  return (
    <View>
      <Text>NotesDevotional</Text>
    </View>
  );
};

export default NotesDevotional;

const styles = StyleSheet.create({});
