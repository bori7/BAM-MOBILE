import { StyleSheet, Text, View } from "react-native";
import React from "react";

const GFA = () => {
  return (
    <View>
      <Text>GFA</Text>
    </View>
  );
};

export default GFA;

const styles = StyleSheet.create({});
