import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SubscriptionDevotional = () => {
  return (
    <View>
      <Text>SubscriptionDevotional</Text>
    </View>
  );
};

export default SubscriptionDevotional;

const styles = StyleSheet.create({});
